import streamlit as st
import os # Додаємо os для перевірки файлу

# --- 1. КОНФІГУРАЦІЯ СТОРІНКИ ---
st.set_page_config(
    page_title="Фізичні Симуляції",
    page_icon="⚛️",
    layout="wide",
    initial_sidebar_state="expanded"
)

st.sidebar.success("Оберіть симуляцію зі списку.")

# --- 3. ВМІСТ ГОЛОВНОЇ СТОРІНКИ ---
st.title("Ласкаво просимо до збірки фізичних симуляцій! ⚛️")
st.info("Цей додаток є інтерактивною лабораторією для вивчення фізичних явищ. Оберіть симуляцію на бічній панелі зліва, щоб почати.")

st.divider()

# Створюємо колонки
col1, col2 = st.columns([2, 1]) # Колонка для тексту вдвічі ширша

with col1:
    st.header("Про проект")
    st.markdown("""
    Цей додаток є колекцією інтерактивних симуляцій, створених за допомогою Streamlit
    в **Прикарпатському національному університеті імені Василя Стефаника**.
    
    Комп'ютерна фізика дозволяє нам "погратися" з законами природи, візуалізувати
    складні процеси та краще зрозуміти, як влаштований наш Всесвіт.
    
    **Використані технології:**
    * **Streamlit** (для веб-інтерфейсу)
    * **NumPy** (для математичних розрахунків)
    * **Plotly** (для інтерактивних графіків)
    * **SciPy** (для розв'язання диференціальних рівнянь)
    """)
    st.warning("Проект перебуває в активній розробці. Нові симуляції додаються.")

# --- 4. КАРТИНКА (ПОВЕРНУЛИ ЇЇ) ---
with col2:
    image_path = "black_hole.jpg"
    
    if os.path.exists(image_path):
        st.image(image_path,
                 caption="Перше зображення Sgr A* в центрі нашої галактики (EHT, 2022).")
    else:
        # Якщо локального файлу немає, спробуємо URL
        st.image("https://upload.wikimedia.org/wikipedia/commons/thumb/c/c2/Black_Hole_Milkyway.jpg/640px-Black_Hole_Milkyway.jpg",
                 caption="Перше зображення Sgr A* в центрі нашої галактики (EHT, 2022).")
        st.error("Картинка не знайдена локально. Спроба завантажити з Інтернету...")

# --- 5. ПРИВІТАННЯ ---
st.toast('Додаток успішно завантажено!', icon='🎉')

