import streamlit as st

# Використовуємо широкий режим
st.set_page_config(layout="wide")

with st.container(border=True):
    st.title("📚 Фізичний довідник")
    st.info("Швидкий доступ до фундаментальних констант та рівнянь.")

    st.header("Фундаментальні фізичні константи")

    col1, col2, col3 = st.columns(3)
    col1.metric("Швидкість світла (c)", "299 792 458", "м/с")
    col2.metric("Стала Планка (h)", "6.626 x 10⁻³⁴", "Дж·с")
    col3.metric("h-bar (ħ)", "1.054 x 10⁻³⁴", "Дж·с")

    col1, col2, col3 = st.columns(3)
    col1.metric("Гравітаційна стала (G)", "6.674 x 10⁻¹¹", "Н·м²/кг²")
    col2.metric("Заряд електрона (e)", "1.602 x 10⁻¹⁹", "Кл")
    col3.metric("Маса електрона (mₑ)", "9.109 x 10⁻³¹", "кг")

    col1, col2, col3 = st.columns(3)
    col1.metric("Маса протона (mₚ)", "1.672 x 10⁻²⁷", "кг")
    col2.metric("Стала Больцмана (k)", "1.380 x 10⁻²³", "Дж/К")
    col3.metric("Електрична стала (ε₀)", "8.854 x 10⁻¹²", "Ф/м")

    st.divider()

    st.header("Фундаментальні рівняння")

    tab1, tab2, tab3 = st.tabs(["Класична механіка", "Електромагнетизм", "Квантова механіка"])

    with tab1:
        st.subheader("Класична механіка")
        st.latex(r"F = m \cdot a \quad (\text{Другий закон Ньютона})")
        st.latex(r"L = T - V \quad (\text{Лагранжіан})")
        st.latex(r"\frac{d}{dt}\left(\frac{\partial L}{\partial \dot{q}}\right) - \frac{\partial L}{\partial q} = 0 \quad (\text{Рівняння Лагранжа})")

    with tab2:
        st.subheader("Електромагнетизм (Рівняння Максвелла у вакуумі)")
        st.code("""
∇ ⋅ E = 0        (Закон Гаусса)
∇ ⋅ B = 0        (Закон Гаусса для магнетизму)
∇ × E = -∂B / ∂t (Закон Фарадея)
∇ × B = μ₀ε₀(∂E / ∂t) (Закон Ампера-Максвелла)
""", language="plaintext")

    with tab3:
        st.subheader("Квантова механіка")
        st.latex(r"i\hbar \frac{\partial}{\partial t}\Psi(r,t) = \hat{H}\Psi(r,t) \quad (\text{Рівняння Шредінгера})")
        st.latex(r"\Delta x \cdot \Delta p \geq \frac{\hbar}{2} \quad (\text{Принцип невизначеності})")
        st.latex(r"\lambda = h / p \quad (\text{Хвиля де Бройля})")
